<h1>Users</h1>
<table class="table">
  <%= for user <- @users do %>
    <tr>
      <td><%= link(user.email, to: user_path(@conn, :show, user)) %></td>
    </tr>
  <% end %>
</table>
<%= link("New User", to: user_path(@conn, :new), class: "new-user") %>
